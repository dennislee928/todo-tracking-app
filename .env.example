# =============================================================================
# Todo Tracking App - Environment Variables
# Copy to .env and fill in values. Do not commit secrets.
# =============================================================================

# -----------------------------------------------------------------------------
# Backend (web-be)
# -----------------------------------------------------------------------------
# Local: sslmode=disable | Supabase: sslmode=require (NOT sslmode=enable - invalid)
DATABASE_URL=postgres://user:password@localhost:5432/todo?sslmode=disable
JWT_SECRET=your-secret-key-change-in-production

# For Supabase: use sslmode=require (NOT sslmode=enable - invalid for PostgreSQL)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_JWT_SECRET=

# Stripe (Web payment)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=

# Apple IAP (iOS)
APPLE_SHARED_SECRET=

# Google Play (Android)
GOOGLE_PACKAGE_NAME=
GOOGLE_SERVICE_ACCOUNT_JSON=

# -----------------------------------------------------------------------------
# Frontend (web-ui)
# -----------------------------------------------------------------------------
# Local: localhost | Production (Fly.io): https://todo-web-be.fly.dev/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# AdSense (optional - for ad display)
NEXT_PUBLIC_ADSENSE_ID=
NEXT_PUBLIC_ADSENSE_SLOT=1234567890

# -----------------------------------------------------------------------------
# Flutter (device_shared) - iOS & Android
# API_URL is passed via: flutter run/build --dart-define=API_URL=https://...
# - Production (Fly.io): https://todo-web-be.fly.dev/api/v1
# - Android emulator: http://10.0.2.2:8080/api/v1
# - iOS simulator: http://localhost:8080/api/v1
# - Real device: http://YOUR_IP:8080/api/v1
# -----------------------------------------------------------------------------
